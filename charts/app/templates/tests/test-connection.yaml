apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nmp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "nmp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nmp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
